@page
@model TimeTracker.AdminUI.Pages.Account.SessionModel
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<TimeTracker.Core.Resources.Errors> Localizer

@{
    ViewData["Title"] = Localizer["MesSessionsTitle"] ?? "Mes Sessions";
    var period = Model.SelectedPeriod;
}

<h2>@ViewData["Title"]</h2>

<div class="card shadow-sm">
    <form method="get" class="row row-cols-lg-auto g-3 align-items-center">
        <div class="col">
            <label>@Localizer["PeriodLabel"] :</label>
            <select asp-for="SelectedPeriod" class="form-select" id="periodSelect" onchange="toggleCustomDates(this.value)">
                <option value="week" selected="@("week" == period)">@Localizer["ThisWeekOption"]</option>
                <option value="month" selected="@("month" == period)">@Localizer["ThisMonthOption"]</option>
                <option value="custom" selected="@("custom" == period)">@Localizer["CustomOption"]</option>
            </select>
        </div>
        <div class="col" id="customDates" style="display:none;">
            <label asp-for="CustomStartDate" class="form-label">@Localizer["FromLabel"]</label>
            <input asp-for="CustomStartDate" type="date" class="form-control" />
            <label asp-for="CustomEndDate" class="form-label">@Localizer["ToLabel"]</label>
            <input asp-for="CustomEndDate" type="date" class="form-control" />
        </div>
        <input type="hidden" asp-for="WeekOffset" />
        <input type="hidden" asp-for="MonthOffset" />
        <div class="col">
            <button type="submit" class="btn btn-primary">@Localizer["SubmitButton"]</button>
        </div>
    </form>
</div>

@if (period == "week")
{
    <div class="d-flex align-items-center gap-2 my-2">
        <form method="get" style="display:inline;">
            <input type="hidden" name="SelectedPeriod" value="week" />
            <input type="hidden" name="WeekOffset" value="@(Model.WeekOffset - 1)" />
            <button type="submit" class="btn btn-link">&laquo; @Localizer["PreviousWeekButton"]</button>
        </form>
        <span>
            @Model.CurrentWeekStart.ToString("dd/MM/yyyy") - @Model.CurrentWeekEnd.ToString("dd/MM/yyyy")
        </span>
        <form method="get" style="display:inline;">
            <input type="hidden" name="SelectedPeriod" value="week" />
            <input type="hidden" name="WeekOffset" value="@(Model.WeekOffset + 1)" />
            <button type="submit" class="btn btn-link">@Localizer["NextWeekButton"] &raquo;</button>

        </form>
    </div>
}
else if (period == "month")
{
    <div class="d-flex align-items-center gap-2 my-2">
        <form method="get" style="display:inline;">
            <input type="hidden" name="SelectedPeriod" value="month" />
            <input type="hidden" name="MonthOffset" value="@(Model.MonthOffset - 1)" />
            <button type="submit" class="btn btn-link">&laquo; @Localizer["PreviousMonthButton"]</button>
        </form>
        <span>
            @Model.CurrentMonthStart.ToString("MMMM yyyy")
        </span>
        <form method="get" style="display:inline;">
            <input type="hidden" name="SelectedPeriod" value="month" />
            <input type="hidden" name="MonthOffset" value="@(Model.MonthOffset + 1)" />
            <button type="submit" class="btn btn-link">@Localizer["NextMonthButton"] &raquo;</button>
        </form>
    </div>
}

@if (Model.Sessions != null && Model.Sessions.Any())
{
    <div class="card mt-4">
        <div class="card-header bg-light">
            <h5 class="mb-0">@Localizer["SessionsForYou"]</h5>
            <div>
                @if (period == "week")
                {
                    <span>
                        @Model.CurrentWeekStart.ToString("dd/MM/yyyy") - @Model.CurrentWeekEnd.ToString("dd/MM/yyyy")
                    </span>
                }
                else if (period == "month")
                {
                    <span>
                        @Model.CurrentMonthStart.ToString("dd/MM/yyyy") - @Model.CurrentMonthEnd.ToString("dd/MM/yyyy")
                    </span>
                }
                else if (period == "custom")
                {
                    <span>
                        @(Model.CustomStartDate?.ToString("dd/MM/yyyy") ?? "—") - @(Model.CustomEndDate?.ToString("dd/MM/yyyy") ?? "—")
                    </span>
                }
            </div>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-light">
                        <tr>
                            <th>@Localizer["SessionTypeLabel"]</th>
                            <th>@Localizer["StartTimeLabel"]</th>
                            <th>@Localizer["EndTimeLabel"]</th>
                            <th>@Localizer["DurationLabel"]</th>
                            <th>@Localizer["TravelTimeEstimateLabel"]</th>
                            <th>@Localizer["StartAddressLabel"]</th>
                            <th>@Localizer["EndAddressLabel"]</th>
                            <th>@Localizer["DinnerPaidLabel"]</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var s in Model.Sessions)
                        {
                            <tr>
                                <td>@Localizer[$"SessionType_{s.SessionType}"]</td>
                                <td>
                                    <span class="dt-local" data-dt="@s.StartTime.ToString("o")"></span>
                                </td>
                                <td>
                                    @if (s.EndTime.HasValue)
                                    {
                                        <span class="dt-local" data-dt="@s.EndTime.Value.ToString("o")"></span>
                                    }
                                    else
                                    {
                                        <span>—</span>
                                    }
                                </td>
                                <td>@(s.WorkDuration.HasValue ? $"{(int)s.WorkDuration.Value.TotalHours}h{s.WorkDuration.Value.Minutes}m" : "—")</td>
                                <td>@(s.TravelTimeEstimate.HasValue? s.TravelTimeEstimate.Value.ToString(@"hh\:mm") : "—")</td>
                                <td>@s.StartAddress</td>
                                <td>@s.EndAddress</td>
                                <td>@Localizer[$"DinnerPaidBy_{s.DinnerPaid}"]</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
else
{
    <p class="text-muted mt-4">@Localizer["NoSessionsMessage"]</p>
}

@section Scripts {
    <script>
        function toggleCustomDates(val) {
            document.getElementById('customDates').style.display = val === 'custom' ? 'flex' : 'none';
        }
        document.addEventListener('DOMContentLoaded', function() {
            toggleCustomDates(document.getElementById('periodSelect').value);
            // Affichage des dates/horaires en heure locale du navigateur
            document.querySelectorAll('.dt-local').forEach(function (el) {
                const raw = el.getAttribute('data-dt');
                if (raw) {
                    const date = new Date(raw);
                    el.textContent = date.toLocaleString();
                }
            });
        });
    </script>
}
